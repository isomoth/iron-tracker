const BASE_URL = 'https://iron-tracker.herokuapp.com';

export const API_URL = (slug) => `${BASE_URL}/${slug}`;

/* export const API_URL = (_id) =>
  `https://iron-tracker.herokuapp.com/foods/id/${_id}`;
 */
